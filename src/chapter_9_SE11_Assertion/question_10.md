設問10

引数paramの値がnullでないことを確認するためのアサーションを記述したい。正しいものを選べ（１つ）


A. assert param == null;


B. assert param != null;


C. assert (param == null);


D. assert (param != null);



#param が null でないことを確認するアサーション」

`assert (param != null);`  

Or


`assert param != null;`  

(※括弧がないだけで意味は同じ)  
